packages:
  - "apps/*"
  - "packages/*"

# pnpm 11+: lifecycle scripts are blocked unless explicitly allowed here.
# Keep this allowlist minimal — only packages that legitimately need native/postinstall builds.
allowBuilds:
  bcrypt: true
  esbuild: true
  sharp: true
