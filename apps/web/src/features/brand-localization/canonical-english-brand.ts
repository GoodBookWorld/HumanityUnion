import { CANONICAL_ENGLISH_BRAND_FALLBACK } from "@hu/types";

/** Re-export builtin English brand — never depends on Gemini. */
export const CANONICAL_ENGLISH_BRAND = CANONICAL_ENGLISH_BRAND_FALLBACK;

export { CANONICAL_ENGLISH_BRAND_FALLBACK };
