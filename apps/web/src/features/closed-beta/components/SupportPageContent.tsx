/**
 * Closed-beta Support content moved to the Launch-ready public Support page.
 * Re-export preserves any residual imports during transition.
 */
export { SupportPageContent } from "../../support/components/SupportPageContent";
