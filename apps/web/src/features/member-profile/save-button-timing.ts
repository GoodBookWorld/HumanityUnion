/**
 * Platform-wide Save-button success hold duration.
 *
 * Lifecycle UX Completion Pack 02 Part 4 — one shared timing configuration
 * for every `useSaveButtonPhase` consumer (Profile + every Lifecycle stage
 * editor). Long enough that "Saved" is perceivable after "Saving...".
 */
/** Design System UX Pack 01 — ~2s success hold (perceivable, not sluggish). */
export const SAVE_BUTTON_SUCCESS_HOLD_MS = 2000;
